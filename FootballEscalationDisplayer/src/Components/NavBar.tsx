import { Link } from "react-router";

const NavBar = () => {
  return (
    <div className="navbar">
      <h1>
        <Link to="/">FCFC</Link>
      </h1>
    </div>
  );
};

export default NavBar;
